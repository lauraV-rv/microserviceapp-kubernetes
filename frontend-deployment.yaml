apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: lauravrv/frontend-app:v1
        ports:
        - containerPort: 80
        env:
        - name: AUTH_API_ADDRESS
          value: "http://auth-service:8000"
        - name: TODOS_API_ADDRESS
          value: "http://todos-service:8082"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
